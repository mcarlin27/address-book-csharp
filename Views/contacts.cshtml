<h1>All Contacts</h1>
@if (@Model.Count == 0)
{
  <h3>No Contacts in your Address Book!</h3>
  <!-- <p><a href="/contacts/new">Add a Contact!</a></p> -->
}
@foreach (var contact in Model)
{
  <h3></contacts/@contact.GetId()>@contact.GetName()</h3>
}
<p><a href="/">Back Home</a></p>
